const Loading = () => {
	return (
		<div className="absolute top-0 right-0 flex items-center justify-center w-full h-screen bg-black">
			<h1 className="text-yellow-500 ">LOADING...</h1>
		</div>
	);
};
export default Loading;
